# Cargo binaries + dependencies
target
cargo

# Local secrets
.env.local

# These are backup files generated by rustfmt
**/*.rs.bk

# Misc files that shouldn't be in source control
.DS_Store
.dump
.vscode
.idea